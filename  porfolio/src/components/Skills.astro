---
const tools = [
  {
    name: 'Ruby on Rails',
    logo: '/rails-logo.svg',
    description: 'My one stop solution for full-stack web development...'
  },
  // ... otros tools
]
---

<div class="py-16 px-4 max-w-6xl mx-auto">
    <h2 class="text-3xl font-bold text-center mb-8">My Favourite Tools</h2>
    
    <div class="space-y-16">
        {tools.map(tool => (
            <div class="flex flex-col md:flex-row gap-8 items-start">
                <img src={tool.logo} alt={tool.name} class="w-32 h-32" />
                <div class="space-y-4 flex-1">
                    <h3 class="text-2xl font-bold">{tool.name}</h3>
                    <p class="text-gray-600 dark:text-gray-300">
                        {tool.description}
                    </p>
                </div>
            </div>
        ))}
    </div>
</div>