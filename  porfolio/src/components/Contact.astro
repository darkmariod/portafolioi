<section class="py-16 max-w-2xl mx-auto px-4" id="contact">
  <h2 class="text-3xl font-bold text-center mb-12">Contacto</h2>
  
  <div class="contact-form">
      <div class="form-group">
          <label>Nombre completo</label>
          <input type="text" class="form-input">
      </div>

      <div class="form-group">
          <label>Correo electrónico</label>
          <input type="email" class="form-input">
      </div>

      <div class="form-group">
          <label>Mensaje</label>
          <textarea class="form-input h-32"></textarea>
      </div>

      <button class="submit-btn">
          Enviar mensaje
      </button>
  </div>
</section>

<style>
  .contact-form {
      @apply bg-white dark:bg-gray-800 p-8 rounded-xl shadow-md space-y-6;
  }

  .form-group {
      @apply space-y-2;
  }

  .form-input {
      @apply w-full px-4 py-2 rounded-lg border dark:border-gray-700 dark:bg-gray-900 focus:ring-2 focus:ring-primary;
  }

  .submit-btn {
      @apply w-full bg-primary text-white py-3 px-6 rounded-lg hover:bg-blue-700 transition-colors font-medium;
  }
</style>