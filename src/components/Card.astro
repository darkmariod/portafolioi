---
const { title, imageURL, desc, href } = Astro.props;
---

<article class="card">
  <img src={imageURL} alt={title} class="w-full h-48 object-cover rounded-t-lg" />
  <div class="p-6">
    <h3 class="text-xl font-bold mb-2">{title}</h3>
    <p class="text-gray-600 dark:text-gray-300">{desc}</p>
    <a href={href} class="mt-4 inline-block text-blue-600 hover:text-blue-800 dark:text-blue-400 dark:hover:text-blue-300">
      Learn more →
    </a>
  </div>
</article>